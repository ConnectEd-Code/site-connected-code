# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: connectedcode-contact-form
metadata:
  template: connectedcode-contact-form
services:
  api:
    project: ./api
    language: py
    host: function
